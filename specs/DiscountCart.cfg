\* Add statements after this line.
\* TLC model configuration for DiscountCart.tla
\* Translate first:  java -cp tla2tools.jar pcal.trans DiscountCart.tla
\* Then check:       java -cp tla2tools.jar tlc2.TLC -config DiscountCart.cfg DiscountCart.tla

SPECIFICATION Spec

\* One item is enough to surface the dropped-discount bug. Try {"a", "b"} for
\* broader coverage once you've seen it.
CONSTANTS
  Items = {"a"}

INVARIANT TypeOK
INVARIANT Consistent     \* this one PASSES: each replica stays well-formed
INVARIANT MergeAgrees    \* this one FAILS: v1 drops v2's discount, totals diverge

\* After you apply a fix from the spec footer, MergeAgrees passes and TLC will
\* report a "deadlock" at the terminal (all-done) state. That is just normal
\* termination - add -deadlock to silence it:
\*   java -cp tla2tools.jar tlc2.TLC -deadlock -config DiscountCart.cfg DiscountCart.tla
